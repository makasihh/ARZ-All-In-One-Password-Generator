{
  "version": 2,
  "builds": [
    { "src": "src/components/PasswordGenerator.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "src/components/PasswordGenerator.py" }
  ]
}
